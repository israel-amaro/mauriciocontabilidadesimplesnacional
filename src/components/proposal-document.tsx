import { Brand } from './brand';
import { PrintButton } from './print-button';
import { brand } from '@/lib/brand';
import type { Lead } from '@/lib/types';
export function ProposalDocument({lead}:{lead:Lead}){
  return <main className="proposal-document"><div className="print-actions"><a href="/admin">Voltar ao painel</a><PrintButton/></div><header><Brand/><span>PROPOSTA PERSONALIZADA<br/>{`MR-${lead.id.slice(0,8).toUpperCase()}`}</span></header><span className="eyebrow">UMA PARCERIA FEITA PARA SEU NEGÓCIO</span><h1>Vamos construir<br/>o próximo capítulo.</h1><div className="proposal-recipient"><div><small>PREPARADA PARA</small><h2>{lead.company}</h2><p>Aos cuidados de {lead.name}</p></div><div><small>DATA DA PROPOSTA</small><p>{new Intl.DateTimeFormat('pt-BR',{dateStyle:'long'}).format(new Date(lead.updatedAt))}</p></div></div><section><h2>O que vamos cuidar juntos</h2><p>{lead.proposal.scope}</p></section><div className="proposal-price"><div><span>HONORÁRIOS MENSAIS</span><strong>{new Intl.NumberFormat('pt-BR',{style:'currency',currency:'BRL'}).format(lead.proposal.amount)}</strong></div><p>Escopo personalizado para<br/>o momento da sua empresa.</p></div>{lead.proposal.notes&&<section><h2>Condições e observações</h2><p>{lead.proposal.notes}</p></section>}<section><h2>Nosso próximo passo</h2><p>Converse com nossa equipe para esclarecer suas dúvidas, alinhar o início do atendimento e formalizar a contratação.</p></section><footer><strong>{brand.company}</strong><p>{brand.email} · {brand.phoneLabel}<br/>{brand.address} · {brand.city}</p></footer></main>;
}
